{
  "name": "smooth-page-transition",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build index.html --out-dir docs --public-url ./",
    "deploy": "npm run build && git add . && git commit -a -m 'latest build' && git push origin master"
  },
  "homepage": "https://slickepinne.github.io/gsap-page-animation",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@dogstudio/highway": "^2.1.3",
    "gsap": "^3.6.0",
    "parcel-bundler": "^1.12.4"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "sass": "^1.23.7"
  }
}
